---
description: 'A sample-app app.'
labels: ['app', 'react']
---

A React application for sample-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use sample-app 
```

To run the app locally, run the following:

```
bit run sample-app 
```
